import {Tile} from "components/tile.slint";
import {Colors} from "components/colors.slint";
import {Button, VerticalBox, StandardTableView , HorizontalBox, GridBox} from "std-widgets.slint";
import {Processes, ProcessesAdapter} from "components/processes.slint";
import {Network, NetworkAdapter, NetworkInterface} from "components/network.slint";
import {IO, IOAdapter, DiskIO, PartitionIO} from "components/io.slint";
import {System, InfoAdapter} from "components/info.slint";
import {CPUInfo, CPUAdapter, CPU} from "components/cpu.slint";
export {ProcessesAdapter, NetworkAdapter, NetworkInterface, IOAdapter, DiskIO, PartitionIO, InfoAdapter, CPUInfo, CPUAdapter}
export component MainWindow inherits Window {
    width: 1400px;
    height: 900px;
    title: "RUSTop";
    background: Colors.palette.background;
    callback sort_ascending_col(int);
    callback sort_descending_col(int);
    GridBox {
        spacing: 5px;
         Row {
                System {height: 225px; width: (root.width - 25px)/ 3;}
                IO {height: 225px; width: (root.width - 25px)/ 3;}
                CPU {height: 225px; width: (root.width - 25px) / 3;}
        }
        Row {
            Processes {colspan: 3; height: 610px;}
        }
        Row {
            Network {colspan: 3;  height: 40px;}
        }
    }
}
